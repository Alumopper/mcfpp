package top.alumopper.mcfpp.tokens;

public class FunctionToken extends Token{
    /**
     * 从一个字符串中解析一个语句
     *
     * @param code 需要解析的字符串
     */
    public FunctionToken(String code) {
        super(code);
    }


}
